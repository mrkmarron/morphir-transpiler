namespace NSMain;

function main(l: List<{price: Float, quantity: Int}>): Float {
    if ((l).empty()) {
        return 0.0f;
    }
    else {
        return (((l).map(fn(e) => e.price)).sum() / ((l).size()).toFloat());
    }
}